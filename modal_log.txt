commit 790daede7cb90a26f3accb3d567f2b3d3f57c037
Author: Dante2526 <naylanmoreira350@gmail.com>
Date:   Wed Jan 28 21:13:48 2026 -0300

    feat: Enhance modal and admin panel functionality
    
    Introduces a new `size` prop to the `Modal` component, allowing for adjustable modal widths ('sm', 'md', 'lg'). This provides better control over modal display.
    
    Additionally, the admin panel within `App.tsx` has been refactored for improved UI and to accommodate a new "Move Employee" option. A new `AdminButton` component is introduced for a cleaner, more consistent button style. The `onMoveUser` callback is now included in the admin panel's props, preparing for its implementation.
    
    Finally, the `types.ts` file now includes `MoveEmployee` in the `ModalType` enum, aligning with the new functionality. The README has also been updated with a placeholder image and local setup instructions.

diff --git a/components/Modal.tsx b/components/Modal.tsx
index 82e24dc..d539b80 100644
--- a/components/Modal.tsx
+++ b/components/Modal.tsx
@@ -7,15 +7,23 @@ interface ModalProps {
   title: string;
   children: ReactNode;
   scale?: number;
+  size?: 'sm' | 'md' | 'lg';
 }
 
-const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale = 1 }) => {
+const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale = 1, size = 'sm' }) => {
   if (!isOpen) return null;
 
   const modalStyle = { 
     transform: `scale(${scale})`, 
     animation: 'fade-in-scale 0.3s forwards ease-out' 
   };
+  
+  const sizeClass = {
+    sm: 'max-w-sm',
+    md: 'max-w-md',
+    lg: 'max-w-lg',
+  }[size];
+
 
   return (
     <div 
@@ -23,7 +31,7 @@ const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale =
       onClick={onClose}
     >
       <div 
-        className="bg-light-card dark:bg-dark-card rounded-2xl shadow-2xl p-8 w-full max-w-sm text-center"
+        className={`bg-light-card dark:bg-dark-card rounded-2xl shadow-2xl p-8 w-full ${sizeClass} text-center`}
         style={modalStyle}
         onClick={(e) => e.stopPropagation()}
       >
@@ -41,4 +49,4 @@ const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale =
   );
 };
 
-export default Modal;
+export default Modal;
\ No newline at end of file

commit ab7e716f25fab71cb9ce45a3ddde62f49148cf6a
Author: Dante2526 <naylanmoreira350@gmail.com>
Date:   Wed Nov 26 11:12:38 2025 -0300

    feat: Simplify modal and remove ReportModal
    
    The generic `Modal` component has been simplified by removing the `maxWidth` prop. Its default `max-w-sm` is now hardcoded, and the `ReportModal` component has been removed entirely. The logic for handling modal scaling on touch devices has also been adjusted.

diff --git a/components/Modal.tsx b/components/Modal.tsx
index eadf67a..82e24dc 100644
--- a/components/Modal.tsx
+++ b/components/Modal.tsx
@@ -7,10 +7,9 @@ interface ModalProps {
   title: string;
   children: ReactNode;
   scale?: number;
-  maxWidth?: string;
 }
 
-const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale = 1, maxWidth = 'max-w-sm' }) => {
+const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale = 1 }) => {
   if (!isOpen) return null;
 
   const modalStyle = { 
@@ -24,7 +23,7 @@ const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale =
       onClick={onClose}
     >
       <div 
-        className={`bg-light-card dark:bg-dark-card rounded-2xl shadow-2xl p-8 w-full ${maxWidth} text-center`}
+        className="bg-light-card dark:bg-dark-card rounded-2xl shadow-2xl p-8 w-full max-w-sm text-center"
         style={modalStyle}
         onClick={(e) => e.stopPropagation()}
       >
@@ -42,4 +41,4 @@ const Modal: React.FC<ModalProps> = ({ isOpen, onClose, title, children, scale =
   );
 };
 
-export default Modal;
\ No newline at end of file
+export default Modal;
